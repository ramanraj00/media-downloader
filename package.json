{
  "name": "media-downloader",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@9.9.4",
  "description": "God-tier media downloader platform",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.0.12",
    "typescript": "^5.5.4",
    "@types/node": "^20.14.15"
  }
}
